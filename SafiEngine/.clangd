# SafiEngine clangd config.
# The engine is pure C11 — no C++ translation units.

CompileFlags:
  Add: [-std=c11, -Wall, -Wextra, -Wpedantic]
  Compiler: clang

---
If:
  PathMatch: .*\.h$
CompileFlags:
  Add: [-xc]

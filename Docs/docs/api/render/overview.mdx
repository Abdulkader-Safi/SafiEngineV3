---
title: Render Overview
description: SDL_gpu, shaders, meshes, materials — the frame lifecycle of SafiEngine.
---

# Render Overview

SafiEngine's renderer is a thin wrapper around the **SDL_gpu** API introduced in SDL 3. SDL_gpu is Khronos-style, explicit, and portable — the same code runs on Metal (macOS/iOS), Vulkan (Linux/Windows/Android), and D3D12 (Windows) without `#ifdef`s.

## Frame lifecycle

```text
safi_renderer_begin_frame(r)
  ├─ SDL_AcquireGPUCommandBuffer
  ├─ SDL_WaitAndAcquireGPUSwapchainTexture
  ├─ (resize depth texture if needed)
  └─ SDL_BeginGPURenderPass  (color + depth)

<user draw calls via SDL_BindGPU…, SDL_DrawGPU…>

safi_debug_ui_render(r)         // ImGui on top
safi_renderer_end_frame(r)
  ├─ SDL_EndGPURenderPass
  └─ SDL_SubmitGPUCommandBuffer
```

Only one render pass per frame in the first-pass engine. Adding more passes (shadow maps, post-processing) is a future evolution.

## Shaders: MSL today, cross-platform later

First milestone shaders are authored in **MSL (Metal Shading Language)** and passed straight to `SDL_CreateGPUShader` via [`safi_shader_create`](./shader). This gets the engine up and running on the Metal backend with zero extra toolchain setup.

Cross-platform shader compilation (one source → SPIR-V for Vulkan and DXIL for D3D12) is a planned follow-up once either (a) a newer SDL_shadercross release fixes its vendored DirectXShaderCompiler build on modern Apple libc++, or (b) we offline-compile HLSL/GLSL → SPIR-V via glslang at build time.

## Coordinate conventions

| Axis  | Meaning                                                         |
| ----- | --------------------------------------------------------------- |
| `+X`  | right                                                           |
| `+Y`  | up                                                              |
| `-Z`  | forward                                                         |
| Wind. | counter-clockwise = front (SDL_GPU_FRONTFACE_COUNTER_CLOCKWISE) |
| Depth | `SDL_GPU_COMPAREOP_LESS`, `clear = 1.0`                         |

These match cglm's `glm_lookat` / `glm_perspective` defaults, so you can hand camera matrices straight to the shader without sign flips.

## See also

- [`SafiRenderer`](./renderer)
- [`SafiMesh`](./mesh)
- [`SafiMaterial`](./material)
- [`safi_shader_compile_hlsl`](./shader)
- [`safi_gltf_load`](./gltf_loader)
